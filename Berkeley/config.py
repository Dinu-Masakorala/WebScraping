import os

# Folder where all downloaded files will be saved
DOWNLOAD_DIR = os.path.join(os.getcwd(), "Property Info")

# Path to your Excel file with TMS numbers
EXCEL_PATH = os.path.join(os.getcwd(), "Master Excel sheet (for task_one).xlsx")
